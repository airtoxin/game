{
  "name": "game",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "wsrun --parallel -c dev",
    "build": "wsrun --stages -c build",
    "test": "wsrun --sequential -c test"
  },
  "devDependencies": {
    "wsrun": "5.2.4"
  }
}
